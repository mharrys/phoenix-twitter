<div class="row">
  <div class="col-lg-4">
    <div class="area-no-click">
      <h4><%= @user.name %></h4>

      <%= if authenticated(@conn) && !user_profile(@conn) do %>
        <%= if @user.follower_id do %>
          <%= button("Unfollow", class: "btn btn-default", to: user_follower_path(@conn, :delete, @user.id), method: "delete") %>
        <% else %>
          <%= button("Follow", class: "btn btn-primary", to: user_follower_path(@conn, :create, @user.id), method: "post") %>
        <% end %>
      <% end %>
    </div>

    <%= render App.SharedView, "user_stats.html", conn: @conn, user: @user, active: "followers" %>
  </div>

  <div class="col-lg-8">
    <div class="area-no-click"><h4>Followers</h4></div>
    <%= for f <- @followers do %>
      <%= render App.SharedView, "user_item.html", conn: @conn, user: f.follower %>
    <% end %>
  </div>
</div>
