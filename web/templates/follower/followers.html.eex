<div class="row">
  <div class="col-lg-12">
    <h2><%= @user.name %>'s followers:</h2>
    <%= for f <- @followers do %>
      <p><a href="<%= user_path(@conn, :show, f.follower.id) %>"> <%= f.follower.name %></a></p>
    <% end %>
  </div>
</div>
